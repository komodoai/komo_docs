---
title: delete
---

### backend delete

The `backend delete` command is used to delete a backend entity.

### Usage

```
komo backend delete [OPTIONS] NAME
```

### Options

```
Options:
  -f, --force:  Delete the backend even if there are jobs/machines that are still running or unreacheable (those will be deleted, but not
cancelled)
```

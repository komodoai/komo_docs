---
title: list
---

## compute list

The `list` command is used to list all compute clusters that have been created with Komodo. To list all compute clusters, simply run the following command:

```
komo compute list
```

This will display a list of all the clusters, along with their details

---
title: create
---

## compute create

The `compute create` command is used to create a Komodo compute cluster.

### Usage

```
komo compute create [OPTIONS] NAME
```

### Options

```
  -b, --backend: Name of the compute cluster type to create.
```

```
  -c, --cluster_id: ID of the cluster to create.  [required]
```

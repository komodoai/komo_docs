## list
The `list` command is used to list all the jobs that have been executed on Komodo. To list all jobs, simply run the following command:

```
komo list
```

This will display a list of all the jobs, along with their status, ID, and creation time.
